$kind: collection
description: |-
  API pathfinding & pengiriman (delivery) dengan engine rute ALT + CH, hierarchical routing (graf gang level-1 dari tile lokal), traffic TomTom, dan manajemen kurir/batch/shipment.

  Gunakan tombol **Authorize** untuk memasukkan token dari `POST /api/v1/auth/login` agar endpoint yang membutuhkan autentikasi dapat diuji.
variables:
  baseUrl: /
auth:
  - id: 6afbca0f-c55d-4619-809e-14a8f48f5cdf
    type: bearer
    name: bearer auth
    credentials:
      token: "{{bearerToken}}"
